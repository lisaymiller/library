package library;

public class ReferenceBook extends Book {

    public ReferenceBook(String title, String author, String publisher, int releaseYear, String genre, String format) {
        super(title, author, publisher, releaseYear, genre, format);
    }
}