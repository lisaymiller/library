package library;

public class ReferenceBook extends Book {

    public ReferenceBook(String title, Author author, PublishingCompany publisher, int releaseYear, String genre,
            String format) {
        super(title, author, publisher, releaseYear, genre, format);
    }
}