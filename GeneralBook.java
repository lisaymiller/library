package library;

public class GeneralBook extends Book {

    public GeneralBook(String title, Author author, PublishingCompany publisher, int releaseYear, String genre,
            String format) {
        super(title, author, publisher, releaseYear, genre, format);
    }

}