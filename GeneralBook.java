package library;

public class GeneralBook extends Book {

    public GeneralBook(String title, String author, String publisher, int releaseYear, String genre, String format) {
        super(title, author, publisher, releaseYear, genre, format);
    }
}